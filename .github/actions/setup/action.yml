name: Setup
description: Install Node from .nvmrc and restore the npm cache.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
      with:
        node-version-file: .nvmrc
        cache: npm
    - shell: bash
      run: npm ci
