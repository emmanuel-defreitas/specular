/** Loaded by `@plugin "../specular.plugin.ts"` in src/index.css. */
import { createPlugin } from "@exegia/specular/plugin"

import { surfaces } from "./specular.config.ts"

export default createPlugin(surfaces)
